const nomes = ['tiago Barbosa', 'Peter mendonça', 'rakel MOREira'];

const nomesAtualizados = nomes.map(nome => nome.toUpperCase());

console.log(`Nomes => ${nomes}`);
console.log(`Nomes atualizados => ${nomesAtualizados}`);